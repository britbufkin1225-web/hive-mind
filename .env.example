# Frontend (Vite) dev config. Copy to `.env` in the repo root.
# 8787 is the canonical local dev backend port (started via `npm run dev:backend`).
# The frontend client falls back to this same value if the var is unset.
VITE_API_BASE_URL=http://localhost:8787/api

